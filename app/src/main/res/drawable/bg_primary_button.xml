<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gedrückter Zustand: heller -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:angle="90" android:endColor="@color/btn_primary_light" android:startColor="@color/btn_primary_light" />
        </shape>
    </item>

    <!-- Normaler Zustand: dunkel -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp" />
            <gradient android:angle="90" android:endColor="@color/btn_primary_dark" android:startColor="@color/btn_primary_bg" />
        </shape>
    </item>

</selector>
